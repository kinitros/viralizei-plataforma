# Servidor API
# Porta do servidor Express local (default: 3001)
PORT=

# Admin
# Token de autenticação para acessar rotas administrativas (ex.: /api/admin/checkout)
ADMIN_TOKEN=
# Senha de administrador para acessar rotas de redirect-links (use uma das chaves abaixo)
ADMIN_REDIRECT_PASSWORD=
ADMIN_PASSWORD=

# Checkout Dinâmico
# Padrão de variáveis para mapear chaves de checkout quando não existe link no store admin.
# Formato:
# - CHECKOUT_<KEY>_<QTY>    (para quantidades específicas)
# - CHECKOUT_<KEY>_DEFAULT  (fallback quando qty não for fornecida)
# Onde <KEY> é a chave do serviço com pontos substituídos por underscores e em UPPERCASE.
# Exemplo: 'instagram.likes.world' -> CHECKOUT_INSTAGRAM_LIKES_WORLD
#          'facebook.views'        -> CHECKOUT_FACEBOOK_VIEWS

# Exemplos de serviços (adicione a URL do checkout como valor):
CHECKOUT_INSTAGRAM_FOLLOWERS_WORLD_DEFAULT=
CHECKOUT_INSTAGRAM_FOLLOWERS_BR_DEFAULT=
CHECKOUT_INSTAGRAM_LIKES_WORLD_DEFAULT=
CHECKOUT_INSTAGRAM_LIKES_BR_DEFAULT=
CHECKOUT_INSTAGRAM_VIEWS_REELS_DEFAULT=
CHECKOUT_INSTAGRAM_VIEWS_STORIES_DEFAULT=

CHECKOUT_TIKTOK_FOLLOWERS_WORLD_DEFAULT=
CHECKOUT_TIKTOK_FOLLOWERS_BR_DEFAULT=
CHECKOUT_TIKTOK_LIKES_WORLD_DEFAULT=
CHECKOUT_TIKTOK_LIKES_BR_DEFAULT=
CHECKOUT_TIKTOK_VIEWS_DEFAULT=

CHECKOUT_YOUTUBE_LIKES_DEFAULT=
CHECKOUT_YOUTUBE_VIEWS_DEFAULT=
CHECKOUT_YOUTUBE_SUBSCRIBERS_DEFAULT=

CHECKOUT_FACEBOOK_FOLLOWERS_WORLD_DEFAULT=
CHECKOUT_FACEBOOK_LIKES_WORLD_DEFAULT=
CHECKOUT_FACEBOOK_VIEWS_DEFAULT=

CHECKOUT_KWAI_FOLLOWERS_BR_DEFAULT=
CHECKOUT_KWAI_LIKES_BR_DEFAULT=
CHECKOUT_KWAI_VIEWS_DEFAULT=

# Exemplos com quantidade específica
CHECKOUT_TIKTOK_VIEWS_1000=
CHECKOUT_INSTAGRAM_FOLLOWERS_BR_1000=

# Supabase (Opcional)
# URL do projeto Supabase
SUPABASE_URL=
# Chave anônima do Supabase (public key)
SUPABASE_ANON_KEY=

# Storage Provider
# Define qual provider de armazenamento usar para redirect links
# Opções: 'supabase', 'file', 'memory'
# - supabase: Usa Supabase como banco de dados (requer SUPABASE_URL e SUPABASE_ANON_KEY)
# - file: Usa arquivos JSON locais (padrão)
# - memory: Usa memória (dados não persistem entre reinicializações)
STORAGE_PROVIDER=file

# Netlify (Opcional)
# Configurações específicas para deploy no Netlify
# Estas variáveis são automaticamente configuradas pelo Netlify, mas podem ser definidas manualmente se necessário
NETLIFY_SITE_ID=
NETLIFY_AUTH_TOKEN=

# Observações:
# - Nunca comite arquivos .env com valores reais.
# - Use um arquivo .env local com valores reais (ex.: .env.local) e mantenha este .env.example apenas como referência.
# - Se um link existir no store admin (api/data/checkout.json), ele tem precedência sobre as variáveis de ambiente.
# - Para usar Supabase, configure SUPABASE_URL e SUPABASE_ANON_KEY e defina STORAGE_PROVIDER=supabase
# - Para deploy no Netlify, configure as variáveis de ambiente no painel do Netlify (Site settings > Environment variables)
# - No Netlify, use STORAGE_PROVIDER=supabase ou memory (file não funciona em ambiente serverless)